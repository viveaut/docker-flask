version: '3'

services:
  flask-app:
     build: .
     ports:
        - "80:80"
     volumes:
        - .:/app
     environment:
        - FLASK_APP=run.py
        - 'RUN=flask run --host=0.0.0.0 --port=80'